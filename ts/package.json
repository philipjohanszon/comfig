{
  "name": "comfig-workspace",
  "private": true,
  "packageManager": "pnpm@9.x",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^5.x",
    "vitest": "^4.1.10"
  }
}